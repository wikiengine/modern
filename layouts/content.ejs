<div class="bg-card text-card-foreground rounded-xl border shadow w-full mt-2 md:w-3/4 md:mt-4 md:ml-4 h-lvh">
    <div class="p-6">
        <div class="flex flex-col md:flex-row md:items-center mb-3">
            <h1 class="font-bold mb-3 md:mb-0 text-xl">
                <%-include('./title')%>
            </h1>
            <div class="self-end md:self-auto md:ml-auto">
                <%-include('./contentTool')%>  
            </div>
        </div>   
        <hr class="uk-hr" />
        <div class="mt-3">
            <div id="content-style" class="content-style">
                <div id="content-body" class="content-body" :class="$store.state.currentTheme === 'dark' ? 'theseed-dark-mode' : 'theseed-white-mode'">
                    <%-contentHtml%>
                </div>
            </div>
            <%if(page.viewName === 'license') {%>
                <div class="mt-3">
                  <h2>Modern License</h2>
                  <pre><%=getSkinFile('LICENSE')%></pre>
                </div>
            <%}%>
            <%if(page.viewName === 'wiki' && page.data.date) {%>
                <%const timeTag = getFullDateTag(page.data.date);%>
                <%if(page.data.rev) {%>
                    <p>이 리비전은 <%-timeTag%>에 편집되었습니다.</p>
                <%} else {%>
                    <p class="footer-info-lastmod">이 문서는 <%-timeTag%>에 마지막으로 편집되었습니다.</p>
                <%}%>
            <%}%>
        </div>
    </div>
</div>